import{_ as t,o,c as n,a0 as e}from"./chunks/framework.CSK6mTG2.js";const d=JSON.parse('{"title":"MTP：一次\\"猜\\"多个词的新范式","description":"","frontmatter":{},"headers":[],"relativePath":"menuAI/multi-token-prediction.md","filePath":"menuAI/multi-token-prediction.md"}'),s={name:"menuAI/multi-token-prediction.md"};function r(p,a,l,i,u,h){return o(),n("div",null,[...a[0]||(a[0]=[e(`<h1 id="mtp-一次-猜-多个词的新范式" tabindex="-1">MTP：一次&quot;猜&quot;多个词的新范式 <a class="header-anchor" href="#mtp-一次-猜-多个词的新范式" aria-label="Permalink to &quot;MTP：一次&quot;猜&quot;多个词的新范式&quot;">​</a></h1><blockquote><p>传统大模型一次只猜一个词，Meta 的新研究说：能不能一次猜多个？猜对了加速，猜错了也没关系。这套方法叫做 Multi-Token Prediction，简称 MTP。</p></blockquote><hr><h2 id="开头-为什么大模型-一个字一个字往外蹦-很慢" tabindex="-1">开头：为什么大模型&quot;一个字一个字往外蹦&quot;很慢 <a class="header-anchor" href="#开头-为什么大模型-一个字一个字往外蹦-很慢" aria-label="Permalink to &quot;开头：为什么大模型&quot;一个字一个字往外蹦&quot;很慢&quot;">​</a></h2><p>用过 ChatGPT 或者 Claude 的同学，应该都注意到了一个现象：输出的文字是<strong>逐渐显现</strong>的，一个字一个字、一句话一句话地往外&quot;吐&quot;。</p><p>这背后不是打字员在打字，而是大模型的<strong>自回归生成机制</strong>在起作用——每生成一个新 token，都要完整跑一遍整个模型的计算。模型越大、生成的文本越长，这个过程就越慢。</p><p>打个比方：想象你在做一个翻译题，传统的方式是<strong>遮住一整句话，只露出一个词，让你猜这个词是什么</strong>。猜完后再遮住下一个，继续猜。这当然准确，但效率很低——因为每次你其实都读懂了整句话的大部分内容，却只能猜一个词。</p><p><strong>MTP 的核心思想</strong>：<strong>能不能一次多猜几个？</strong> 就像把遮住的词从1个变成3个、4个，让你一次预测多个位置。</p><hr><h2 id="投机解码-先猜再验证" tabindex="-1">投机解码：先猜再验证 <a class="header-anchor" href="#投机解码-先猜再验证" aria-label="Permalink to &quot;投机解码：先猜再验证&quot;">​</a></h2><p>在说 MTP 之前，先介绍一个更直观的概念——<strong>投机解码（Speculative Decoding）</strong>，它和 MTP 其实是&quot;同一个思想的两个版本&quot;。</p><h3 id="猜测-验证框架" tabindex="-1">猜测-验证框架 <a class="header-anchor" href="#猜测-验证框架" aria-label="Permalink to &quot;猜测-验证框架&quot;">​</a></h3><p>投机解码的思路分两步：</p><ol><li><strong>快速猜测</strong>：用一个小的&quot;草稿模型&quot;（Draft Model），一次快速生成多个候选词</li><li><strong>批量验证</strong>：用大模型（Target Model）一次验证这些候选词——如果验证通过，就一次性&quot;接受&quot;这一串词；如果某个词不通过，就从那个位置开始重新猜</li></ol><p>你可以理解为：助理先帮你写了一句话，你只需要快速读一遍确认没问题就通过了，不用自己重写每个字。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统方式（Target Model 单独跑）：</span></span>
<span class="line"><span>输入 → 计算1 → 词1 → 计算2 → 词2 → 计算3 → 词3 → 计算4 → 词4 → ...</span></span>
<span class="line"><span>（每步都要完整计算，慢）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>投机解码：</span></span>
<span class="line"><span>草稿模型快速猜：词1 → 词2 → 词3 → 词4（几个词一次猜出来）</span></span>
<span class="line"><span>Target Model 验证：[词1 ✓][词2 ✓][词3 ✗] → 停止，从词3重新开始</span></span>
<span class="line"><span>→ 一次性接受了词1和词2，省去了2次完整计算</span></span></code></pre></div><p><strong>为什么这个方法work？</strong> 核心假设是：<strong>草稿模型在大多时候是对的</strong>。因为语言生成有局部规律性（&quot;今天天&quot;后面大概率是&quot;气&quot;，&quot;你好&quot;后面大概率是&quot;啊&quot;），小模型也能猜对这些&quot;简单&quot;的部分。只有遇到复杂推理、罕见知识时才出错，这时候再用大模型来&quot;兜底&quot;。</p><p>实际效果：通常能实现 <strong>2~4倍推理加速</strong>，且输出质量<strong>和直接用大模型完全一致</strong>（因为验证的是大模型的判断）。</p><hr><h2 id="mtp-把-猜测-内置到模型里" tabindex="-1">MTP：把&quot;猜测&quot;内置到模型里 <a class="header-anchor" href="#mtp-把-猜测-内置到模型里" aria-label="Permalink to &quot;MTP：把&quot;猜测&quot;内置到模型里&quot;">​</a></h2><p>投机解码需要两个模型（一大一小），部署起来比较麻烦。<strong>Multi-Token Prediction（MTP）</strong> 的做法更优雅：<strong>把&quot;一次猜多个词&quot;这件事，直接训练进同一个模型里</strong>。</p><h3 id="训练目标-从-猜1个-到-猜多个" tabindex="-1">训练目标：从&quot;猜1个&quot;到&quot;猜多个&quot; <a class="header-anchor" href="#训练目标-从-猜1个-到-猜多个" aria-label="Permalink to &quot;训练目标：从&quot;猜1个&quot;到&quot;猜多个&quot;&quot;">​</a></h3><p>传统语言模型的训练目标是：<strong>给定前N个词，预测第N+1个词</strong>。</p><p>MTP 的训练目标是：<strong>给定前N个词，同时预测第N+1、N+2、N+3...个词</strong>，用多个独立的&quot;预测头（Predictor Head）&quot;分别负责不同位置的预测。</p><p>你可以理解为：不是派一个人每次猜一个词，而是<strong>同时派3个人，每个人负责猜不同位置的词</strong>。第1个人猜下一个词，第2个人猜下两个词，第3个人猜下三个词——他们的预测结果会互相作为参考。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>传统目标：预测 Token[t+1]</span></span>
<span class="line"><span>MTP 目标：同时预测 Token[t+1], Token[t+2], Token[t+3], ...</span></span>
<span class="line"><span>         ↓</span></span>
<span class="line"><span>    训练3个独立的预测头</span></span></code></pre></div><h3 id="mtp-的两种主流实现-vanilla-vs-eagle" tabindex="-1">MTP 的两种主流实现：Vanilla vs Eagle <a class="header-anchor" href="#mtp-的两种主流实现-vanilla-vs-eagle" aria-label="Permalink to &quot;MTP 的两种主流实现：Vanilla vs Eagle&quot;">​</a></h3><p>MTP 不是一种固定算法，目前主流有两条技术路线：<strong>Vanilla MTP</strong> 和 <strong>Eagle（有时也叫 MTP-Eagle）</strong>。</p><h4 id="vanilla-mtp-最直觉的做法" tabindex="-1">Vanilla MTP：最直觉的做法 <a class="header-anchor" href="#vanilla-mtp-最直觉的做法" aria-label="Permalink to &quot;Vanilla MTP：最直觉的做法&quot;">​</a></h4><p>Vanilla MTP 的预测头是<strong>串行连接</strong>的：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>输入 → 主模型 → 预测头1（猜t+1）→ 预测头2（基于头1的结果猜t+2）→ 预测头3（基于头2的结果猜t+3）</span></span></code></pre></div><p>这和自回归生成的顺序一样，只不过把计算&quot;压缩&quot;了——每次不是跑完整的主模型，而是只跑预测头。</p><p><strong>优点</strong>：结构简单，容易理解 <strong>缺点</strong>：预测头之间有依赖关系，t+3 的预测依赖 t+1 和 t+2 的结果，一旦前面错了后面也会错（误差累积）</p><h4 id="eagle-mtp-eagle-并行猜测-更激进" tabindex="-1">Eagle（MTP-Eagle）：并行猜测，更激进 <a class="header-anchor" href="#eagle-mtp-eagle-并行猜测-更激进" aria-label="Permalink to &quot;Eagle（MTP-Eagle）：并行猜测，更激进&quot;">​</a></h4><p>Eagle 的核心改进是——<strong>让所有预测头并行工作，不依赖彼此的结果</strong>。</p><p>怎么做到的？它用的是<strong>更深的预测头结构</strong>，让每个预测头自己就能&quot;看到&quot;足够的信息来做预测，而不是依赖前一个预测头的输出。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>输入 → 主模型 → 预测头1（猜t+1） 预测头2（猜t+2） 预测头3（猜t+3）</span></span>
<span class="line"><span>                ↑            ↑            ↑</span></span>
<span class="line"><span>              都从主模型的隐藏状态获取信息，独立预测</span></span></code></pre></div><p><strong>为什么 Eagle 更快？</strong> 因为推理时，Eagle 的每个预测头可以<strong>同时运行</strong>（并行），不需要等前一个头完成。这比 Vanilla 的串行结构理论上快了接近3倍。</p><p><strong>DeepSeek-V3</strong> 采用的就是 Eagle 风格的 MTP 架构，所以它成为了第一个在生产环境大规模使用 MTP 的主流模型。</p><hr><h2 id="mtp-在实际推理中怎么用" tabindex="-1">MTP 在实际推理中怎么用 <a class="header-anchor" href="#mtp-在实际推理中怎么用" aria-label="Permalink to &quot;MTP 在实际推理中怎么用&quot;">​</a></h2><p>MTP 的训练目标变了，但<strong>推理时的用法</strong>才是关键。目前有三种使用方式：</p><h3 id="方式一-完整-mtp-推理-最激进" tabindex="-1">方式一：完整 MTP 推理（最激进） <a class="header-anchor" href="#方式一-完整-mtp-推理-最激进" aria-label="Permalink to &quot;方式一：完整 MTP 推理（最激进）&quot;">​</a></h3><p>推理时，模型同时用所有预测头来生成，每次生成多个 token。这需要推理框架（如 vLLM、SGLang）做特殊适配。</p><p><strong>理论加速</strong>：3~4倍（取决于预测头数量） <strong>代价</strong>：实现复杂度高，需要框架支持</p><h3 id="方式二-投机解码-保守但实用" tabindex="-1">方式二：投机解码（保守但实用） <a class="header-anchor" href="#方式二-投机解码-保守但实用" aria-label="Permalink to &quot;方式二：投机解码（保守但实用）&quot;">​</a></h3><p>训练一个 MTP 模型，用它的预测头来辅助一个更大的模型做投机解码。这是最常见的部署方式——不需要改动推理框架，只需要把 MTP 模型当&quot;草稿模型&quot;用。</p><p>DeepSeek-V3 就是这样用的：MTP 预测头作为&quot;小模型&quot;加速主模型的推理。</p><h3 id="方式三-辅助训练目标-最常用" tabindex="-1">方式三：辅助训练目标（最常用） <a class="header-anchor" href="#方式三-辅助训练目标-最常用" aria-label="Permalink to &quot;方式三：辅助训练目标（最常用）&quot;">​</a></h3><p>很多模型在训练时会加入 MTP 作为<strong>辅助损失函数</strong>，帮助模型学得更好，但推理时仍然用传统自回归。这种方式不会加速推理，但能提升模型质量。</p><p>打个比方：就像学生做题时用&quot;一题多解&quot;来加深理解（MTP 训练），但考试时还是用标准方法答题（推理时不依赖 MTP）。</p><hr><h2 id="mtp-为什么有效-几个关键原因" tabindex="-1">MTP 为什么有效：几个关键原因 <a class="header-anchor" href="#mtp-为什么有效-几个关键原因" aria-label="Permalink to &quot;MTP 为什么有效：几个关键原因&quot;">​</a></h2><h3 id="_1-更丰富的训练信号" tabindex="-1">1. 更丰富的训练信号 <a class="header-anchor" href="#_1-更丰富的训练信号" aria-label="Permalink to &quot;1. 更丰富的训练信号&quot;">​</a></h3><p>传统训练每个 token 只有一个标签（对或错）。MTP 提供了<strong>多个预测目标</strong>，每个预测头都在独立学习&quot;下一个位置应该是什么&quot;。</p><p>类比：传统训练像是每次只练习一个填空题；MTP 像是一次性做3道关联的填空题，你对全局的理解会更深。</p><h3 id="_2-强迫模型做-规划" tabindex="-1">2. 强迫模型做&quot;规划&quot; <a class="header-anchor" href="#_2-强迫模型做-规划" aria-label="Permalink to &quot;2. 强迫模型做&quot;规划&quot;&quot;">​</a></h3><p>当预测头需要预测 t+3 的位置时，它必须理解&quot;如果要生成到这个位置，前面几个词应该怎么排列&quot;。这逼迫模型在生成之前就在内部做某种&quot;规划&quot;。</p><h3 id="_3-减少-重复生成-问题" tabindex="-1">3. 减少&quot;重复生成&quot;问题 <a class="header-anchor" href="#_3-减少-重复生成-问题" aria-label="Permalink to &quot;3. 减少&quot;重复生成&quot;问题&quot;">​</a></h3><p>传统模型在生成长文本时容易陷入重复循环（&quot;今天天气很好，天气很好...&quot;）。MTP 因为有多个预测头同时工作，单个预测头的局部重复会被其他预测头&quot;纠正&quot;，整体上减少了这个现象。</p><hr><h2 id="局限性-mtp-不是银弹" tabindex="-1">局限性：MTP 不是银弹 <a class="header-anchor" href="#局限性-mtp-不是银弹" aria-label="Permalink to &quot;局限性：MTP 不是银弹&quot;">​</a></h2><p>说了这么多 MTP 的好处，也得聊聊它的局限：</p><p><strong>1. 预测头数量有上限</strong></p><p>预测头不是越多越好。目前主流是2~4个头，超过这个范围收益递减，而且每个头都有参数量，增加太多会让模型变大。</p><p><strong>2. 预测头之间存在一定误差累积</strong></p><p>即使 Eagle 用并行设计，预测头之间仍然有相关性。当一个位置预测错误时，后续预测的准确率会下降。这也是为什么大多数系统仍然需要在&quot;验证&quot;步骤中用大模型兜底。</p><p><strong>3. 对短文本没有明显优势</strong></p><p>如果生成的文本只有几个词（比如回答一个&quot;是/否&quot;问题），MTP 的开销反而比传统方法高。</p><hr><h2 id="总结-一次多猜几个-是一种思维方式" tabindex="-1">总结：一次多猜几个，是一种思维方式 <a class="header-anchor" href="#总结-一次多猜几个-是一种思维方式" aria-label="Permalink to &quot;总结：一次多猜几个，是一种思维方式&quot;">​</a></h2><p>MTP 的本质，是对&quot;生成效率&quot;这个问题的一次范式级改进：</p><ul><li><strong>投机解码</strong> = 外部加一个小模型来猜</li><li><strong>Vanilla MTP</strong> = 把猜测能力内嵌到模型里，但预测头串行工作</li><li><strong>Eagle MTP</strong> = 把猜测能力内嵌，且预测头并行工作，效率最高</li></ul><p>DeepSeek-V3 的实践已经证明：<strong>MTP 在生产环境是可行的，而且确实能带来 1.8~3 倍的真实加速</strong>。</p><p>对于前端同学来说，理解 MTP 的意义不在于自己实现它，而在于理解大模型推理加速的思路：<strong>不是让大模型跑得更快，而是用小模型替代大模型做它能做的事，把大模型留给真正需要它的时刻</strong>。这个思路在未来会越来越常见。</p>`,75)])])}const q=t(s,[["render",r]]);export{d as __pageData,q as default};
